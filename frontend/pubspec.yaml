name: hummor_app
description: Hummor / MindSignal Flutter client.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/mindsignal_logo.png
    - assets/images/google_icon.png
    - assets/images/doctor_patient_illustration.png
    - assets/images/background_pattern.png
